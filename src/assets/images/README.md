# Image Assets

Place application images in this folder and import them from React components:

```tsx
import heroImage from '../../assets/images/hero-main.jpg'
```

Use `public/images` only when an asset must be referenced by a fixed URL path.
